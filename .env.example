# GraphQL API (noma-platform-core)
VITE_GRAPHQL_URL=http://localhost:4000/graphql

# Sentry (optional — leave blank to disable in local dev)
VITE_SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=development

# Sentry source map upload (CI / production builds only)
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=
